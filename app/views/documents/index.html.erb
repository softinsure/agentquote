<h1>Listing documents</h1>

<table>
  <tr>
  </tr>

<% for documents in @documents %>
  <tr>
    <td><%= link_to 'Show', documents %></td>
    <td><%= link_to 'Edit', edit_documents_path(documents) %></td>
    <td><%= link_to 'Destroy', documents, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New documents', new_documents_path %>
